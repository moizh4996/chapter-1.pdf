  // 1. Declare 3 variables in one statement
    let x = 5, y = 10, z = 15;

    // 2. Declare 5 legal variable names
    let myVar, _name, $amount, age1, userName;

    // 5 illegal variable name examples (These are comments, not real code, because they'd cause errors)
    // let 1name;
    // let my-name;
    // let var;
    // let alert;
    // let @price;

    alert("Check the browser page for JS variable rules.");